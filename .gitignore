# dependencies
node_modules
.pnp
.pnp.js

# next
.next
/out
next-env.d.ts

# production
/build
/.next/standalone

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env
.env
.env.local
.env.*.local
!.env.example

# prisma
src/generated

# excel data (too big, imported on deploy)
*.xlsx
!prisma/seed-data.xlsx
# rooms export writes into this template at runtime — must ship with the code
!data/rooms-template.xlsx

# local SQLite database — real student data, never commit
betzel.db
betzel.db-journal
betzel.db-shm
betzel.db-wal
backups/

# build cache
tsconfig.tsbuildinfo

# Claude / agent workspace
.claude/

